# GT1 经 DEBUG 连接复测（2026-09-17 14:39）

用户要求测试并确认 GT1 接在 DEBUG 板，沿用之前已确认的目标 USB 单路供电方式。

- 14:39:40 确认 DEBUG 010、无会话/USBKEY/过流，单次设置 USB 路由 pc、目标供电 ON，两条命令成功。
- 恢复脚本 `artifacts/gt1-check-20260917-1439.connect.json` 因未检测到完整正常 GT1 根设备退出 1；供电动作成功不等于目标枚举成功，没有重发。
- COM5 以 3 Mbaud 接收 120 秒（包括上电前准备时间），831246 字节，保存在 `artifacts/gt1-check-20260917-1439.uart.log`。记录 97 次 GT1 0.2.124-dev 应用启动、96 次 `P33_VDDIO_LVD_RST`。目标反复在蓝牙初始化/trim 附近重启。
- 未启动 Flutter 功能测试，未向 GT1 发送应用协议命令、未写参数、未重烧。
- 上电后与采集结束后的 DEBUG 查询均为 target_power=true、usb_route=pc、overcurrent=false；usb_resets 始终为 9，control_errors=0，overcurrent_trips=0。
- 本轮未观察到 DEBUG 再次安全关闭输出；与此前 DEBUG 接口代次增加并关闭输出的情况不同。本轮复现 GT1 电源相关复位，不能归因于应用扫描或参数请求。
- 控制 ON 不是实际电压测量。尚不能区分 USB/DEBUG 供电链路、板上稳压/负载与固件电源配置；未定位具体损坏元件。需要带载电压测量或已验证供电条件的对照实验。
- 最终保持目标供电 ON、路由 pc。没有盲目重复开关、关闭保护或更改固件。
