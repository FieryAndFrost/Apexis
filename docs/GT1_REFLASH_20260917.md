# GT1 重烧记录（2026-09-17）

用户明确要求重新烧录，并确认 GT1 已接回 DEBUG 目标 USB，仅由该口供电。

## 已完成

- 使用此前已实机验证的 `0.2.124-dev` 预编译库固件。下载前 `package_firmware.py --build build-debug-prebuilt --verify` 返回 0。
- ELF SHA256：`eb021dcf1de5963e31ef8533924ba1381452b8f01e17c126497cd8abf8655b5d`。
- `jl_isd.fw` SHA256：`a476438115e8089212d7e90fcd64bde1bd8e0b67d45f265ce978282f7f212996`。
- DEBUG 身份 `AC7911B8 / 010 / JLD00010`，无过流、无活动会话和 USBKEY 动作。
- 单次 USBKEY 流程：半周期 10 us，7 包后检测到应答；Windows 确认 GT1 BOOT 为 `4C4A:3442 / BR27 UBOOT1.00`，物理位置为历史 GT1 端口 `PCIROOT(0)#PCI(0803)#PCI(0000)#USBROOT(0)#USB(1)#USB(4)#USB(2)`。BOOT 工具退出 0。
- 下载前再次确认仅一台杰理 BOOT 根设备，型号及位置匹配 GT1。未向 DEBUG 板写入固件。
- 使用产品 wrapper 的 `--download`，退出 0。日志包含实际 sector/block 写入、`Download completed.`、重启；Flash 2 MiB，UUID 与 9 月 16 日同一测试机匹配。
- `ERASE MODE: NONE`，未执行整片格式化、恢复出厂或音色导入。正常烧录仍写入固件区域，不表示完全没有扇区擦写。
- 11:33:29 独立 PnP 查询确认 GT1 正常根设备 `3654:4D55`、`SINCO-MIDI`、`SINCO-AUDIO` 均为 OK。此前重启后的短时查询尚未出现，原始过程不隐去。
- 最后 DEBUG 回读目标供电开启、路由 pc、无过流，USB 接口代次为 2；保持供电和电脑直通。

## 证据与限制

- `artifacts/gt1-reflash-20260917-1133.boot.json`：完整 BOOT 命令及拓扑证据；其中 `target_flash_written=false` 仅表示 BOOT 工具自身不写 Flash，实际后续下载见 stdout。
- `artifacts/gt1-reflash-20260917-1133.stdout.log`、`.stderr.log`：实际下载日志。
- `artifacts/gt1-reflash-20260917-1133.uart.log`：COM5、3 Mbaud、120 秒仅接收，0 字节；不能声称取得固件启动串口证明。未通过串口向目标发送数据。
- 本轮开始时不能连接 GT1，未取得最新参数备份。历史备份保留；不承诺最新未保存音色恢复。
- 在写入前的正常直通阶段曾发现 SINCO-AUDIO，因此不能将本次恢复解释为已证明“原程序全部丢失”。
- 应用协议只读复核失败：`flutter drive --no-pub --profile -d windows --driver=test_driver/hardware_readonly.dart --target=integration_test/hardware_readonly_test.dart --dart-define=HARDWARE_AUDIT=true --dart-define=MIDI_DEVICE=SINCO-MIDI`，退出 1。在设备扫描阶段找不到精确 MIDI 端点，没有进入参数查询循环，不能宣称三次连接或功能查询通过。
- 11:35 后 PnP 再次确认 GT1 根设备和 MIDI 接口不在场；根设备 `DEVPKEY_Device_IsPresent=false`，LastArrivalDate/LastRemovalDate 均出现 11:35:07，表明重新枚举后又移除。不是仅 APP 名称匹配失败。
- 掉线后 DEBUG 仍回读 `target_power=true / usb_route=pc / overcurrent=false / usb_resets=2`。只能证明控制状态，不能证明目标实际电压稳定，也不能单凭此排除线缆或固件问题。
- 本轮已完成实际烧录，但未完成稳定通信恢复；未盲目重复刷写。已请用户确认 GT1 UART 日志线和共地仍连接，以取得启动/掉线证据。
- 旧只读报告已另存 `artifacts/hardware-readonly-before-reflash-20260917-1133.json`，旧成功结果不作为本轮验证结果。

## 串口接回后的启动诊断

- 用户确认串口接好后重新采集：`artifacts/gt1-postflash-startup-20260917-1136.uart.log`。
- 原计划断电重启，但前置只读检查发现目标已经 OFF/disconnect，断言停止，未发送断电动作。随后使用受保护的恢复工具单次设置 pc/ON；记录 `artifacts/gt1-postflash-startup-20260917-1136.restore.json`。该工具因检查当刻枚举不完整退出 1，不代表两条控制命令失败；没有重发。
- DEBUG 接口代次已从 2 变为 4，恢复后维持 4，供电控制 ON、路由 pc、无过流。此次状态变化原因未取得 DEBUG 独立 PC9 日志，不猜测用户动作或 Windows 挂起。
- 新日志明确多次进入 `app_core task entered GT1 0.2.124-dev`，CPU1 启动，随后在蓝牙初始化/trim 附近重新从 setup_arch 开始。首轮为 `P33_VDDIO_POR_RST`，后续反复为 `P33_VDDIO_LVD_RST`。
- 平台 `sdk/interface/driver/power/power_reset.h` 定义 `P33_VDDIO_LVD_RST` 为 VDDIO 低压复位、上电复位（电池/vpwr供电）。这是目标电源相关复位的证据，不是 Flash 镜像丢失的证据；未观察到足以归因于普通应用异常或看门狗的日志。
- 低压标志不能单独定位到线材、DEBUG 供电开关、GT1 稳压电路或电源配置。`vbat=64 mV` 出现在无电池接线下，不能冒充 USB 5 V 的实测电压。下一步需要带载测量目标 USB 输入和板上电源轨。
- 未继续刷写或关闭低压保护；当前仍保留目标供电与 PC 直通，稳定连接验收未通过。
