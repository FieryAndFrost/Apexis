# GT1 开启与短时观察（2026-09-17 11:51）

用户明确授权开启目标供电和 USB 直通。未烧录、未修改音色、未发送目标 MIDI 写入命令。

- 11:51:34 DEBUG 010 身份、无会话/USBKEY/过流检查通过，单次 `usb.route(pc)` 和 `target.power(true)` 成功。4 秒后供电 ON，路由 pc，USB 接口代次计数 3。
- `artifacts/gt1-poweron-20260917-1151.json` 枚举验证成功，工具退出 0。独立 PnP 也确认 GT1 的根设备、SINCO-AUDIO、SINCO-MIDI 均 OK。
- `artifacts/gt1-poweron-20260917-1151.uart.log` 为 COM5 3 Mbaud 的仅接收采集，60 秒采集窗口内有约 35 秒目标运行日志，11736 字节。1 次固件 0.2.124-dev 启动、1 次上电复位、0 次 P33_VDDIO_LVD_RST，持续音频统计至运行时间 35.617 秒。没有证明长期稳定。
- 随后的 Flutter 只读检查退出 1，失败于精确 SINCO-MIDI 端点为空，未进入通信查询循环。该失败不是成功报告。
- 独立 PnP/WinMM 枚举确认 GT1 已不在场，仅 DEBUG MIDI；GT1 最后到达 11:51:35，最后移除 11:52:17。
- 最终 DEBUG 回读 target_power=false、usb_route=disconnect、overcurrent=false、usb_resets=5，无会话、无控制错误，UART RX 累计 11907 字节且 TX=0。
- USB 接口代次从 3 增至 5，输出又变为安全关闭状态。此次没有发送目标关闭命令；尚未取得 DEBUG PC9 独立事件日志，不能判定触发来源是主机重置、挂起、用户拔插或其他情况。
- 本轮证据不同于此前反复低压复位：本次已采集区间没有低压复位，随后 DEBUG 输出关闭。最终目标处于 OFF/disconnect；未盲目重复开启或重新烧录。
