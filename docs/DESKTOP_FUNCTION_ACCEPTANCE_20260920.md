# 桌面端本地功能验收（2026-09-20）

## 范围

按用户要求排除手机端、云端及扩展服务。覆盖 Windows 应用的设备连接、同步、编辑、音色、工具、设置和本地文件功能。普通功能、固件负载边界、真实声音、掉电持久化分别报告，不以模拟测试替代实机结果。

## 本轮已执行

- `flutter test --no-pub test local_packages/flutter_midi_command_windows/test`：123 项通过，退出码 0。覆盖已有协议/状态机、文件传输、音色与效果模拟流程、控件/布局及原生 MIDI 包单元测试。既有测试套件含手机布局用例，本轮未开展手机设备联调。
- `flutter analyze --no-pub`：No issues found，退出码 0。
- 最初只枚举 DEBUG。14:44 核验 DEBUG 身份、无过流/活动烧录会话后恢复目标 USB 供电及 PC 路由，GT1 `3654:4D55` 的音频和 MIDI 接口正常枚举。WinMM 实际端口名为 `2 - SINCO-MIDI`，不能固定匹配旧的 `SINCO-MIDI`。未烧录或复位设备。
- `artifacts/gt1-desktop-write-20260920-port2/`：GT1S/GT1B 备份 CRC 和独立整库回读通过；输出静音、同值写不增加版本、输入/PAN/BPM/名称、部分全局参数/USB 音量/EQ/A4、完整 PATCH 写入和旋钮映射检查均通过，共 6 项。
- 14:48:29 进入效果链测试后 **Profile 构建的 Windows 应用崩溃**，事件 1000 / `0xc0000005`，不是测试正常完成。最后一项效果操作未持久记录，不能判为通过。崩溃后 GT1 串口仍有周期统计，PnP 正常，不能据此认定下位机死机或丢固件。
- 崩溃转储：`C:/Users/15111/AppData/Local/CrashDumps/apexis.exe.7508.dmp`；对应 AOT 文件保存为上述目录 `crashed-app.so`。14:54 的恢复工具 Profile 构建也在连接读取当前音色阶段崩溃，尚未写入。
- 同一恢复工具 Debug 构建可正常连接；首次恢复前的二次整库备份被 `STALE error=8` 中止。记录 `artifacts/gt1-desktop-recovery-debug-20260920/`。后续先保存当前 GT1S，再暂时关闭两路物理映射，执行受保护的恢复与独立校验；以恢复报告为准，不将崩溃后的未恢复状态称作完成。
- `artifacts/gt1-desktop-recovery-quiet-20260920/report.json`：14:58:39 完成恢复，`quietBankRestoredVerified=true`、`restored=true`；原始 GT1S（包含两路物理映射）比对通过，恢复后 30 次/30 秒查询通过，测试退出码 0。

## Windows Profile 崩溃修复

- `integration_test/parameter_profile_test.dart` 使用纯 `FirmwareDemo`，不扫描、连接或写入任何物理设备，修复前仍复现原生崩溃，排除了本次崩溃必须由 USB/GT1 故障触发的假设。
- 带调试符号的 AOT 构建及调试器现场：`artifacts/parameter-profile-symbols-20260920/app.windows-x64.symbols`、`artifacts/parameter-profile-cdb-capture-20260920.log`、`artifacts/parameter-profile-full-20260920.dmp`。指令偏移 `0x316CAA` 符号化到 `EffectUnit.parameter` / `ApexisController._loadParameterView` 原第 867 行，即缓存复用分支的参数读取。
- 原实现使用独立 `reuse` 布尔变量携带可空 `cache` 的有效性，跨 `await` 后在此 Windows AOT 路径发生空指针访问。改为有效缓存本身保持可空、各使用点直接检查 `cache != null`；不取消缓存优化，也不改变通信协议或刷固件。未据此宣称所有编译器版本均存在此问题。
- 修复后原先必崩的 Windows Profile 回归通过：6 个 UNIT、3 轮选择/读参/修改/缓存刷新，无真实 MIDI 连接；现有 11 项参数刷新测试通过，相关文件静态分析通过。

## 基本功能复测与 Looper 兼容

- `artifacts/gt1-desktop-write-fixed-20260920/`：直接整库导出遇 `STALE`，保护性中止；未进行参数写入。测试改为先保存并校验 GT1S，只放行临时关闭两路映射的 20 B 写入；整库导出和独立回读通过后才放行其他功能写入。恢复时先以静态映射验证整库，再恢复原始映射。
- `artifacts/gt1-desktop-write-quiet-fixed-20260920/`：实际 Profile 构建已通过效果添加/参数/旁路/重复 TYPE/排序/删除、完整 PATCH、GT1S 半途取消不改变数据、调音器和鼓机启停；未再发生原生崩溃。完整流程未通过：Looper 在已录音、已播放后的一次 `04/01/21` 只读查询收到 `error=1`，后续叠录/撤销未执行。本次已完成停止、清空测试录音、全部修改恢复及整库验证，`restored=true`、`transportFailed=false`。
- 源码核对：`lib_music_tools/src/looper/looper_psram.c::looper_psram_get_progress` 在 worker/audio 更新快照期间返回 `-2`，头文件明确要求稍后再查询；GT1 `product_sysex.c` 的最终错误映射将非零值统一转成 `error=1`。协议日志本身无法区别 `-1` 不可用和 `-2` 暂忙，故不无限吞掉 error 1。
- 上位机 `readLooperProgress()` 为 0.2.124 的该只读快照增加最多 4 次查询（30/60/90 ms 间隔），显式 BUSY=7 同样支持；其他固件的 error 1、其他错误、超时及已变化/失效会话不重试。持续失败仍上报。没有重发录音、播放、撤销等非幂等指令，没有刷写或修改固件。
- 新增 5 项单元回归：临时拒绝恢复、持续拒绝限次、未知版本不误判、BUSY/不支持分类、断开取消待重查，全部通过。完整实机重测以 `artifacts/gt1-desktop-write-final-20260920/` 为准。
- 最终实机复测已退出 0：**20 项通过、1 项明确跳过、0 项失败**，`allChecksPassed=true`、`restored=true`、`quietBankRestoredVerified=true`、`transportFailed=false`。Looper 录音→停止→播放→叠录→撤销/重做→清空完整流程通过；实际出现过一次快照 error 1，有限只读重查后成功。另有两次参数文字标签不支持 error 6，按既有数值回退处理，不是通信丢包。
- 跳过项是当前 F 区音色的直接 SAVE，符合禁止直接保存厂商位置的规则；U 区 SAVE 已由上一轮扩展实测覆盖。
- 修复后的完整离线套件 **129 项通过**，`flutter analyze --no-pub` 无问题。最终 PnP 的 GT1 音频/MIDI 和 DEBUG 均为 OK；未重烧固件。
- UART 证据：`artifacts/gt1-desktop-final-20260920.uart.log`，180 秒采集正常结束。持续收到周期音频统计；存在 `timer_no_response` 和 `RXCSRP_DataError` 告警，不能将此记录描述为“所有日志无异常”，也不能把告警直接认定成设备复位。长时稳定性及 USB 告警仍需单独排查。
- 收尾 DEBUG 健康查询：目标供电 ON、路由 PC、无过流，控制错误/CDC 配置错误/过流计数均 0，`usb_resets=2` 与测试前一致。正常 `lib/main.dart` Windows Profile 应用已重新构建成功，不把集成测试入口留作交付应用；串口采集已结束并释放 COM5。

## U 区和全局设置实机结果

`artifacts/gt1-desktop-extended-20260920/report.json`：15:05:30 完成，全部步骤通过，`restored=true`，无测试错误和恢复错误。

- 原始 GT1S 保存后，临时禁用物理映射；GT1B 导出 CRC + 独立整库回读一致。
- F→U126 另存、U126→U127 复制、两处重命名、交换、RAM 保存回执、两处目录名称/dirty flag 均通过。交换后当前选中从 127 跟随内容变为 126，名称仍为 `AUDIT_U_B`。
- 45 次全局字段写入及独立回读通过：输出路由、Looper 位置/混音/联动和录音偏好、各音量/MIDI 通道、BPM/同步、四段 EQ、A4、USB 录音模式、自动关机开关等。
- 鼓型目录、鼓机/Looper 联动鼓型选择及独立回读通过。已安装模型资源目录返回 0；未测试不存在的模型加载。
- 完整静态整库恢复并逐字节校验，随后恢复原始当前 GT1S 和两路映射。未软关机，不将 SAVE ACK 判作掉电持久化通过。
- 实机及固件协议确认：交换时当前音色应跟随内容。同步修正演示设备此前“停留原地址”的不一致行为，并补选中任一端、未选中两端及同地址 no-op 回归。

## 实机验收范围清单（逐项以执行记录为准）

| 功能组 | 验收点 | 前置条件或边界 |
| --- | --- | --- |
| 连接与同步 | 扫描、身份、首次同步、断开重连、状态回读、目录分页 | 当前固件/设备身份重新核验 |
| 测试前备份 | GT1S、GT1B、CRC、独立整库回读 | 备份落盘且一致后才允许写入；STALE 不绕过 |
| 参数编辑 | 输入/输出/PAN/BPM/名称、同值写、连续编辑、完整 PATCH 分包 | 保存原值，独立 READ 比对 |
| 效果链 | 添加、替换、参数、旁路、清尾音开关、重复实例、排序、删除 | 先轻量算法，已知重载死机单独进行 |
| 音色管理 | F→U 另存、U 重命名/保存/复制/交换、选择跟随、目录 flags | 先完整备份被覆盖位置；不可把 RAM 回执当持久化 |
| 全局与 EQ | 输出路由、录音模式/混音、音量、MIDI 通道、同步、EQ 各字段及 JSON 文件 | 改动后恢复；声音是否正确另行听测 |
| 鼓机 | 目录、鼓型、音量/BPM/同步/空间、播放停止 | 查真实后台状态，不以 ACK 当已播放 |
| Looper | 录放、停止、叠录、撤销/重做、清空、音量/位置/联动、定长/拍号/预备拍/自动录音 | 先确认无需保留的录音；自动触发需输入信号 |
| 调音器 | 进入/退出、A4 设置、无音高、实际输入音符和指针 | 音高准确性需要已知频率的输入信号 |
| 设置/资源 | 旋钮映射、自动关机开关、身份/链接、安装资源目录和选择 | 无模型资源时不能标记加载成功；15 分钟关机另测 |
| 文件与默认值 | GT1S/GT1B 导入导出、取消、错误文件拒绝、恢复出厂后还原备份 | 此前 6 次直连恢复及一次恢复默认通过，本轮重新记录 |
| 持久化 | U 区保存后正常软关机、再次开机、整库核验 | 须可正常软关机/开机；USB 直接拔电不能替代 |
| 压力与故障 | 长时运行、重载算法、异常断连、意外参数通知 | 不循环重发结果未知的写入；失效会话不盲目恢复 |
| 实际音频 | 鼓机可听、Looper 内容、路由/USB 录音、各效果听感 | 需要音频输入/输出采集或用户听测，不能从协议推断 |

## 历史结果与当前结果的区别

9 月 16 日多项基本写入与鼓机/Looper 流程曾实机通过；9 月 18 日恢复默认固件缺失代码段已修复；9 月 20 日直连完成 6 次备份恢复。它们可作基线，但不冒充本轮全部功能已完成。重负载不响应、无人操作仍有参数变化、F/U 持久化和实际音频仍有未闭环项。

GT1 枚举已恢复；Windows Profile 参数崩溃修复及 Looper 暂忙兼容已通过本轮实机复测，U 区/全局设置已通过本轮实机检查，测试数据已还原。尚未完成：真实音频输入/输出和 USB 录音听测、音高精度、自动录音音频触发、正常软关机持久化、15 分钟自动关机、长时稳定性/重载及全部算法组合。零模型资源不能验收模型加载；当前固件是 NO_BT 诊断版，不宣称蓝牙音频已通过。桌面文件对话框未逐个手动点击，文件协议/取消/校验和已有界面自动化已覆盖。手机、云端及扩展按用户要求排除。
