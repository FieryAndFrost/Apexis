# GT1 重烧与通信验证（2026-09-18）

用户明确指定重烧 GT1 效果器主板，不烧 DEBUG 板。沿用之前确认的 DEBUG 目标 USB 单路供电及物理接线。

## 烧录前发现

- DEBUG 身份读取正常：AC7911B8、010、构建 Sep 14 2026 14:49:40、SCSI 控制、COM5。
- `system.status` 为 `target_power=false / usb_route=disconnect / overcurrent=false`。目标断电和数据路径断开足以解释当前没有 GT1 枚举，不能据此认为固件丢失。
- 在连续 UART 接收已开启后，受保护工具单次设置 PC 路由并打开目标供电。GT1 正常枚举，日志出现 `GT1 0.2.124-dev`、`DIAG NO-BT` 和 `APP services started`，证明烧录前程序仍在运行。
- 保存证据 `artifacts/gt1-reflash-20260918-1120.connect.json` 和 `.uart.log`。文件名中的 1120 不代表实际采集时间；实际开始约 13:50，以 JSON 时间为准。

## 备份限制

只读备份两次尝试：

- `artifacts/gt1-backup-20260918-1351/`
- `artifacts/gt1-backup-20260918-1353/`

均导出 386 字节单音色文件，整库 GET 分片 `00/01/35` 被设备以 error 08（STALE）拒绝，期间收到参数变化通知。两份报告均 `verified=false`、`mutationsStarted=false`。这些不是经过整库一致性校验的最新完整备份；历史备份保留。没有将部分数据拼装成成功的 GT1B，也没有导入旧备份覆盖设备。

## 固件与实际烧录

- 使用此前已经构建和实机验证的 NO-BT 诊断版，不在本轮修改固件源码。
- `build-debug-no-bt` 的 `package_firmware.py --verify` 通过。
- ELF SHA256：`e8ded168d9ce8812d03874f658f1b7bbf668cf8fea22462be7183693914a4dab`。
- `jl_isd.fw` SHA256：`59b7c5a1215423d043a6bcf30b64029680c75d5cca612b4c3fb6bdb5498e2194`。
- USBKEY 单轮 10 us 半周期，6 包后 ACK；Windows BOOT 身份 `4C4A:3442`，位置为 `PCIROOT(0)#PCI(0803)#PCI(0000)#USBROOT(0)#USB(1)#USB(4)#USB(2)`，与本轮恢复正常枚举和历史测试机一致。
- BOOT 工具退出 0，`boot_verified=true / completed_without_error=true`。证据 `artifacts/gt1-reflash-20260918-1353.boot.json`。
- 下载前再次确认唯一杰理 BOOT 根设备为 GT1 BR27、物理位置匹配、无其他下载器，复核镜像 SHA256。
- 使用产品 wrapper `python -B tools/package_firmware.py --build build-debug-no-bt --download`，未使用 `-format`。
- 厂商日志记录 Flash 2 MiB、与历史一致的 UUID、实际 Write sector / Write block、`Download completed.`、设备重启；`ERASE MODE: NONE`。这是实际烧写，不是只生成固件包。
- 外层 PowerShell 没有取得 Start-Process 子进程的 ExitCode，因而外层守卫报错；不能声称下载器退出码为 0。未因此重复烧录。下载完成依据厂商实际写入日志、重启 UART、USB 重新枚举及后续协议验证。
- 下载证据 `artifacts/gt1-reflash-20260918-1354.stdout.log`、`.stderr.log`（后者 0 字节）。

## 烧录后验证

- UART 记录软件重启，进入 `GT1 0.2.124-dev`、CPU1 online、NO-BT 诊断标记、`APP services started`。
- Windows SINCO-MIDI、SINCO-AUDIO、GT1 根设备均为 OK。
- Flutter 只读实机检查退出 0，连续连接 3 次成功；版本均为 0.2.124-dev，revision 分别 15、16、16，每次当前视图与独立 READ 一致，TYPE 目录 71 项。
- 24 项检查均为 `ok`，包括系统、连接状态、全局参数/EQ、鼓机、Looper、调音器、旋钮映射及文件状态；读取 128 个音色名称成功。
- 本轮报告 `artifacts/gt1-reflash-20260918-1355.readonly.json`；旧报告另存 `artifacts/hardware-readonly-before-reflash-20260918-1355.json`。
- 最终 DEBUG 查询供电 ON、USB 路由 PC、无过流、无活动会话，control_errors / cdc_config_errors / overcurrent_trips 均为 0。保留目标供电和电脑直通，未重启 DEBUG 板。
- 补充 UART 采集 `artifacts/gt1-reflash-20260918-1355.post.uart.log`。短时间运行观察不是长期稳定性保证。

## 仍需区分的边界

- 蓝牙初始化仍被诊断开关跳过，BLE / Classic / A2DP 暂不可用。查询蓝牙状态成功不意味着蓝牙连接可用。
- 没有执行恢复出厂、音色导入或写入功能复测，不能宣布恢复出厂崩溃、满 DSP 负载、音频听感或掉电持久化问题已解决。
- 未保证最新未保存参数可恢复；没有完整的本轮整库备份。
- 这次失联已观察到目标电源/路由关闭，但其最初为何关闭未在本轮定位，不能仅凭重新烧录将原因归为固件丢失。
